from typing import List, Optional


def recognize(target_image_path: str, candidate_image_paths: List[str]) -> Optional[str]:
    """
    Placeholder local face recognition.

    Compares the face in `target_image_path` to each image in `candidate_image_paths` and
    returns the path of the most similar image. If none are similar (or not implemented),
    return None.

    For now, this function is intentionally not implemented and always returns None.
    """
    return None
